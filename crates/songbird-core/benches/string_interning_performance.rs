//! String Interning Performance Benchmarks - DISABLED
//!
//! This benchmark suite is temporarily disabled during canonical modernization
//! as the string_interning module needs to be rewritten using canonical patterns.

// Temporarily disabled for canonical modernization
// The string_interning module needs to be rewritten to use canonical patterns

#[cfg(feature = "string-interning-benchmarks")]
mod disabled_benchmarks {
    // Original benchmark code would go here
    // Will be re-enabled after string_interning module modernization
}

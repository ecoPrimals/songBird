[package]
name = "songbird-process-env"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Safe process environment overlay (zero unsafe, Rust 2024)"
keywords = ["environment", "process", "overlay", "thread-safe"]
categories = ["os"]

[dependencies]
# Zero external dependencies — std::sync::Mutex + OnceLock only.

[lints]
workspace = true

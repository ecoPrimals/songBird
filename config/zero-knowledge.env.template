# 🍼 Zero-Knowledge Environment Configuration
# Generated by hardcoding elimination script

# Service Identity
SERVICE_ID=my-service
SERVICE_CAPABILITIES=compute,storage  # What THIS service provides

# Required Capabilities (what this service needs)
REQUIRED_CAPABILITIES=security,storage
OPTIONAL_CAPABILITIES=ai,analytics

# Network Configuration (NO hardcoded ports)
SERVICE_PORT=8080
HEALTH_PORT=8081
METRICS_PORT=8082

# Discovery Configuration
ENABLE_INFANT_DISCOVERY=true
DISCOVERY_TIMEOUT_SECS=30

# Capability Endpoints (discovered dynamically or configured)
AI_PROVIDER_ENDPOINT=http://localhost:8080
AiProvider_ENDPOINT=http://localhost:8080
CACHE_PROVIDER_TYPE=<value>
COMPUTE_PROVIDER_ENDPOINT=http://localhost:8080
CONTAINER_ORCHESTRATOR_TYPE=<value>
CONTAINER_RUNTIME_TYPE=<value>
ComputeProvider_ENDPOINT=http://localhost:8080
DATABASE_TYPE=<value>
DOCUMENT_STORE_TYPE=<value>
HEALTH_PORT=8080
KEY_VALUE_STORE_TYPE=<value>
METRICS_PORT=8080
SECURITY_PROVIDER_ENDPOINT=http://localhost:8080
SERVICE_PORT=8080
SERVICE_REGISTRY_TYPE=<value>
STORAGE_PROVIDER_ENDPOINT=http://localhost:8080
SecurityProvider_ENDPOINT=http://localhost:8080
StorageProvider_ENDPOINT=http://localhost:8080
